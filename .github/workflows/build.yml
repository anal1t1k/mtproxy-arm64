name: Build MTProxy ARM64

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Test ARM64
        run: |
          echo "Architecture:"
          uname -m
          echo "Kernel:"
          uname -a
          echo "Compiler:"
          gcc --version
